[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "boukensha"
version = "0.0.0"
description = "Boukensha agent — Step 5: The Agent Loop"
requires-python = ">=3.9"
dependencies = []

[tool.setuptools.packages.find]
include = ["boukensha*"]
